# Process-Inf-values-in-drought-index-series
This function processes a standardized drought index series by: i) Optionally truncating outliers based on a specified range in standard deviation units, and ii) Replacing `Inf` and `-Inf` values according to the 99th and 1st percentiles, respectively.
